export const environment = {
  production: true,
  // Replace with your deployed Render backend URL once you have it
  // (see the Deployment Guide section in SETUP.md).
  apiUrl: 'https://your-ecoloop-api.onrender.com/api',
};
